services:
  postgres:
    image: postgres:17
    container_name: algorent-postgres

    environment:
      POSTGRES_DB: algorent
      POSTGRES_USER: algorent
      POSTGRES_PASSWORD: algorent

    ports:
      - "5433:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: